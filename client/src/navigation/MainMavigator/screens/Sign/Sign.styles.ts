import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  contentContainer: {
    padding: 16,
    justifyContent: 'space-around',
  },
  connectContainer: {
    marginTop: 16,
  },
});
