import { Dimensions } from 'react-native';
const dims = Dimensions.get('window');

export const SCREEN_HEIGHT = dims.height;
export const SCREEN_WIDTH = dims.width;
export const ACTIVITY_INDICATOR_SIZE = 40;
